plugins {
    id 'groovy'
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.3.11'
    //testCompile group: 'junit', name: 'junit', version: '4.12'
    testCompile group: 'org.testng', name: 'testng', version: '7.4.0'
    testCompile group: 'com.google.code.gson', name: 'gson', version: '2.8.6'
    testCompile group: 'org.apache.httpcomponents', name: 'httpclient', version: '4.5.12'
}

//tasks.withType(Test){
//    scanForTestClasses = true
//    include "**/*Test.class"
//}

println("Api Weather Tests!")
println System.getenv("HOME2")

test{
    useTestNG() { suiteXmlFiles << new File(rootDir, "testng.xml") }
}